# notas:

Mi primer app con node

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es la base para la tabla de multiplicar    [número] [requerido]
  -l, --listar   Muestra la tabla que va a grabar    [booleano] [defecto: false]
  -h, --hasta    hasta que numero va la tabla             [número] [defecto: 10]
```